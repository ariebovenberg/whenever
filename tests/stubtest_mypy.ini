# Used by `make typecheck` when running stubtest. The project's own mypy
# settings check the reference implementation in a stricter mode than
# stubtest can build in.
[mypy]
ignore_missing_imports = True
