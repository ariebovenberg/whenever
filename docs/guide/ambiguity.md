---
orphan: true
---

# Ambiguity in timezones

This guide has moved to {ref}`resolving-local-times`, which now covers both
repeated/skipped local times and conflicts between numeric offsets and named
timezones.
